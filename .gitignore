node_modules/
dist/
.claude/
*.local

# Android build outputs
android/app/build/
android/.gradle/
android/build/

# Release signing keys (never commit!)
*.jks
*.keystore

# Build scripts / logs (local tooling)
build_android.bat
build_android.log
build_android.ps1
gen_keystore.ps1

# PocketBase — entire backend folder (contains secrets + binary)
backend/pocketbase.exe
backend/pocketbase.zip
backend/pb_data/
